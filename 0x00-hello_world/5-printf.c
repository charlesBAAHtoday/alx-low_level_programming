#include <stdio.h>

/**
 * main - the main function is where the work starts - just being funny!
 *
 * Return: 0 when no errors are encountered or successful
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art");
return (0);
}
